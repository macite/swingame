int32_t  current;
int32_t  minimum;

int32_t  increase  ( int32_t  to_increase,const char * to_decrease ) 
{
	int32_t  maximum;
	
	maximum = (10);
	minimum = (0);
	current = (minimum+10/(5-1));
	current = (increase  ( (current),("string argument!") ) );
	write_ln  ( (current) ) ;
	while ( ((current==5)) )
	{
		write_ln  ( (5+5) ) ;
		maximum = (5);
		
	}
	if ( ((maximum>minimum)) )
	{
		while ( ((10+(5-2))) )
		{
			write_ln  ( (10) ) ;
			
		}
		
	}
}

